{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <h1>Restaurer le mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
    <div class="button">
        <div class="padding"></div>
        <button type="submit">
            Restaurer le mot de passe
        </button>
    </div>
    {{ form_end(resetForm) }}
{% endblock %}
